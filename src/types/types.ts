export interface IProduct {
    id: number,
    img: string,
    name: string,
    description: string,
    price: number
};

export interface CardProps {
    image: string;
    name: string;
    description: string;
    price: number;
};